spring:
  application:
    name: srvc-uams
  cloud:
      config:
        uri: http://config:7103
        fail-fast: true
        retry:
          initial-interval: 2000
          max-interval: 10000
          multiplier: 2
          max-attempts: 10
